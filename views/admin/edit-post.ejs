<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Approve/Reject</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">  
</head>
<body class="container py-4">
<hr>
  <a href="/dashboard" class="btn-link mb-3 text-decoration-none">&larr; Back</a>
<br>
<br>
  <form action="/edit-post/<%= data._id %>?_method=PUT" 
        method="POST" 
        class="d-flex flex-column flex-sm-row align-items-start align-items-sm-center gap-2 w-100">

    <label for="status" class="form-label mb-0"><b>Status</b></label>

    <select name="status" id="status" class="form-select w-100 w-sm-auto">
      <option value="pending" <%= data.status === 'pending' ? 'selected' : '' %>>Pending</option>
      <option value="approved" <%= data.status === 'approved' ? 'selected' : '' %>>Approved</option>
      <option value="rejected" <%= data.status === 'rejected' ? 'selected' : '' %>>Rejected</option>
    </select><br>

    <div class="d-flex flex-column flex-sm-row gap-2">
  <form action="/edit-post/<%= data._id %>?_method=PUT" method="POST">
    <input type="submit" value="Update" class="btn btn-primary w-100 w-sm-auto">
  </form>

  <form action="/delete-post/<%= data._id %>?_method=DELETE" method="POST">
    <input type="submit" value="Delete" class="btn btn-danger w-100 w-sm-auto">
  </form>
</div>
  </form>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
